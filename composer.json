{
	"name": "genesis-starter",
	"type": "project",
	"license": "MIT",
	"config": {
		"preferred-install": "dist",
		"vendor-dir": "vendor"
	},
	"repositories": [
		{
			"type": "composer",
			"url": "https://wpackagist.org"
		}
	],
	"scripts": {
		"post-install-cmd": [
			"npm install && gulp"
		]
	},
	"require": {
		"php": ">=5.6",
		"composer/installers": "^1.3",
		"a7/autoload": "^2.0"
	}
}